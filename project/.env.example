# Copy this file to .env and fill in your key to use live mode (run.sh --live).
# Cached mode (the default, used for grading) does NOT need a key.
# .env is gitignored -- never commit a real key.
ANTHROPIC_API_KEY=sk-ant-your-key-here
